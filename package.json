{
  "name": "internquest-admin-workspace",
  "private": true,
  "scripts": {
    "debug:build-check": "node scripts/debug-build-check.mjs",
    "fe:install": "npm --prefix frontend install",
    "fe:dev": "npm --prefix frontend run dev",
    "fe:build": "npm --prefix frontend run build",
    "fe:preview": "npm --prefix frontend run preview",
    "fe:lint": "npm --prefix frontend run lint",
    "be:install": "npm --prefix backend/functions install",
    "be:serve": "npm --prefix backend/functions run serve",
    "be:deploy": "npm --prefix backend/functions run deploy",
    "be:logs": "npm --prefix backend/functions run logs"
  }
}
